# needs to be slightly edited to work in Docker instance
# add port 8080 for Apache Traffic Server to URLs

regex_map http://.*:8080/shutdown http://127.0.0.1/403
regex_map http://.*:8080/ http://127.0.0.1/
